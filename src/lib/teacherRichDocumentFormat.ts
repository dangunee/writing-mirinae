/** Stable discriminator for writing.corrections.rich_document_json (html-v1). Kept tiny so server code avoids importing the full teacherRichDocument module. */
export const TEACHER_RICH_FORMAT_HTML_V1 = "html-v1" as const;
